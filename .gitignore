# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# testing
coverage/
playwright-report/
test-results/

# misc
*.tsbuildinfo
.DS_Store
Thumbs.db
*.rar
*.log
npm-debug.log*

# deploy / editor
.vercel/
.idea/
.vscode/

# source footage from the abandoned video concept (kept on disk, not in git)
assets/

# résumé PDFs — kept on disk, deliberately never published. The repo is
# public and a full ATS résumé carries more personal data (phone, address)
# than a portfolio needs to hand out.
*.pdf

# third-party skill installs (symlinked into .claude/skills)
.agents/
.claude/skills/*
!.claude/skills/find-skills

# lighthouse artifacts
.lighthouseci/

# env
.env
.env.*
!.env.example
